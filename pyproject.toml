[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "hve-forge"
version = "0.1.0"
description = "Local-first, durable AI coding harness MVP"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
hve-forge = "hve_forge.cli:main"

[tool.setuptools.packages.find]
include = ["hve_forge*"]
